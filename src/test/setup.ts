// Vitest global setup. Loaded automatically via vite.config.ts → test.setupFiles.
//
// `@testing-library/jest-dom/vitest` registers DOM matchers (toBeInTheDocument,
// toHaveAttribute, etc.) onto Vitest's expect.

import '@testing-library/jest-dom/vitest';
